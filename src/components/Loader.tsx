import React from "react";
import { HashLoader } from "react-spinners";

const Loader = () => {
	return (
		<div className="loader--wrapper">
			<HashLoader color="#35D7B7" />
		</div>
	);
};

export default Loader;
